import mock from 'mock-fs';
import { expect } from 'chai';

import generateScss from 'lib/generate/scss';

describe('SCSS file generator', () => {
  const data = {
    tokens: [],
    globals: [],
  };
  const outputDirectory = './output';
  it('writes the globals file', () => {

  });

  it('writes the atomic level files', () => {

  });

  it('writes a main file', () => {

  });
});
